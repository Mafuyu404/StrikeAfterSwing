org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Shared mod metadata. Every target project reads this file first, then its own
# targets/<loader>-<minecraft-version>/gradle.properties for loader and version values.
mod_id=strikeafterswing
mod_name=StrikeAfterSwing
mod_license=GNU GPL 3.0
mod_version=1.0.0
mod_group_id=cc.sighs
mod_authors=Tower of Sighs
mod_description=Defers mob attacks that land during an attack swing so they resolve after it.

# Shared publishing project IDs. Set these locally, or supply the matching
# CURSEFORGE_PROJECT_ID and MODRINTH_PROJECT_ID environment variables.
# publish_curseforge_project_id=
# publish_modrinth_project_id=
