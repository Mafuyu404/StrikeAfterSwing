pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.neoforged.net/releases/' }
        mavenCentral()
    }
}

rootProject.name = 'StrikeAfterSwing-neoforge-26.1.2'

include 'common'
project(':common').projectDir = file('../../common')
