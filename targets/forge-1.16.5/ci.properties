# Required CI descriptor for this target. New target directories need this file.
# Disabled: ForgeGradle 4.1 needs net.minecraft:mappings_official:1.16.5, which Forge's
# maven no longer serves (404), so a cold-cache build fails in createMcpToSrg. The MCP
# snapshot channel still resolves its mappings but then hits an unrelated ForgeGradle 4.1
# bug (MinecraftUserRepo.findSrgToMcp writes into a directory it never creates). The
# target still builds where an existing ForgeGradle MCP cache is present, so keep it
# excluded until ForgeGradle 4.1 or the mapping artifact is fixed upstream.
ci.enabled=false
ci.java=8
ci.attempts=3
