# Legacy Build Reference

These files preserve the mixed-loader root build that existed before the target split.
They are retained only as migration reference and are not part of the active build.

Use the independent projects under `../targets/` for development and builds.
